# Optimized dependencies for Day 1 and Day 2 notebooks
# Install once with: pip install -r requirements.txt
# Versions pinned to avoid dependency resolution issues

# Core LangChain ecosystem (pinned versions for compatibility)
langchain-core==0.3.21
langchain==0.3.7
langchain-openai==0.2.9
langgraph==0.2.45

# LLM and API
openai==1.54.0
python-dotenv==1.0.1

# Data processing and validation
pydantic==2.9.2
pandas==2.2.3
numpy==1.26.4

# Machine learning (for clustering/deduplication)
scikit-learn==1.5.2

# Type hints
typing-extensions==4.12.2

# Note: jupyter, notebook are assumed to be pre-installed in your environment
# Removed: matplotlib, seaborn, ipywidgets (not used in Day 1 or Day 2)
